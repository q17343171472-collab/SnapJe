<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External files directory for camera photos -->
    <external-files-path
        name="pictures"
        path="Pictures" />
    
    <!-- Cache directory for temporary files -->
    <cache-path
        name="cache"
        path="." />
    
    <!-- Files directory for shared files -->
    <external-files-path
        name="external_files"
        path="." />
    
    <!-- External storage for broader access -->
    <external-path
        name="external_storage"
        path="." />
</paths>
